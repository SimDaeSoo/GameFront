import Vue from 'vue'
import Vuex, { Store } from 'vuex'

Vue.use(Vuex);

export const store: Store<{}> = new Vuex.Store({
    // Data
    state: {
    },
    getters: {
    },
    mutations: {
    }
});